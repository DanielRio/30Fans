@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Sair", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Admin", "LogOn", "Account") ]
}
